

## Plan : Rendre le texte informatif école plus visible

**Fichier** : `src/pages/PlanningMainOeuvre.tsx`

Transformer le simple paragraphe en une alerte stylisée avec un fond bleu clair, une bordure, et une icône — similaire à l'alerte "Planning non validé" déjà présente sur la page, mais en style informatif (bleu/indigo). Utiliser un `div` avec des classes comme `bg-blue-50 border border-blue-200 rounded-lg p-3 text-blue-800` pour qu'il soit impossible à rater.

